"""tests 包标记(使 ``tests.helpers.*`` 可作为 dotted path 被配置/技能加载器 import)。

目录按 src/agent_os 子系统对应:kernel / providers / tools / logic / context /
sidecars / telemetry / cli / web / examples;公共 mock 与组装件在 ``tests/helpers/``。
"""
